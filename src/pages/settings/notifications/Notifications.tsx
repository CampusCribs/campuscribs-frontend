import React from "react";

const Notifications = () => {
  return (
    <div>
      Notifications Email notifications Marketing updates Activity alerts Weekly
      summaries Push notifications Enable/disable Mobile vs Desktop In-app
      notifications Toggle types (e.g., mentions, messages)
    </div>
  );
};

export default Notifications;
